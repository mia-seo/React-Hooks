import React from "react";
import ReactDOM from "react-dom/client";
import AppInput from "./customHooks/AppInput";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<AppInput />);
